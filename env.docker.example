# 複製為 .env.docker 後供 docker compose 使用：
#   cp env.docker.example .env.docker
#
# 必填：JWT_SECRET；寄信請填 SMTP_PASSWORD（應用程式專用密碼）

JWT_SECRET=請改為至少32字元的隨機密鑰
# 對外網址（與瀏覽器列一致；經 Caddy 走 80/443，勿寫 :5001）
# Caddy 已同時啟用 HTTP(80) 與 HTTPS(443，自簽 tls internal)。若全站以 https 存取，請改為 https://你的網域或 IP
SITE_URL=http://localhost
# 若主機 80/443 已被占用，可改映射埠（例如 8080:80、8443:443）
HTTP_PORT=80
HTTPS_PORT=443

# --- SMTP（驗證信、報告、審核通知）---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_TLS=true
SMTP_USERNAME=your_smtp_login@gmail.com
SMTP_PASSWORD=your_app_password_here
DIGEST_FROM_EMAIL=your_smtp_login@gmail.com
NOTIFY_FALLBACK_TO=your_smtp_login@gmail.com
