# 複製為 .env.rclone 置於專案根目錄（遠端 ~/actcweb/.env.rclone），勿提交版本庫。
# 啟用備份成功後，以 rclone 上傳到 OneDrive（RCLONE_REMOTE 須與 rclone config 的遠端名稱一致）
RCLONE_UPLOAD=1
RCLONE_REMOTE=onedrive
RCLONE_PATH_PREFIX=actcweb-backups
# 若 rclone 設定不在預設路徑，可取消註解：
# RCLONE_CONFIG=/home/youruser/.config/rclone/rclone.conf
