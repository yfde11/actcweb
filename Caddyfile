# 本機 / 內網 Docker：Caddy 僅在容器內聽 80/443，對外埠由 docker-compose 的 HTTP_PORT/HTTPS_PORT 映射。
# HTTPS 使用內部 CA 憑證（瀏覽器會顯示不安全，按「進階」繼續即可）。
# 公網 Let's Encrypt 請參考 Caddyfile.production.example。

{
	auto_https off
	servers {
		protocols h1 h2
	}
}

:80 {
	encode gzip
	reverse_proxy web:5001
}

:443 {
	tls internal
	encode gzip
	reverse_proxy web:5001
}
