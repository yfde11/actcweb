# ACTC 考試系統 - 完整項目計劃
## 2026年5月 - 邁向 100% 完成

**計劃發佈日期**: 2026-05-03  
**目標完成日期**: 2026-06-15  
**總工時估算**: 240-280 小時 (4-5 週)

---

## 執行摘要

### 目前狀態
- 考試系統整體進度：75/100 (75%)
- 後端 API：95% 完成（所有核心端點已實現）
- 前端考試管理後台：100% 完成
- 前端題庫管理：85% 完成
- **會員端作答流程：50% 完成**（最大工作量）
- 統計與報表：40% 完成（後端完整，前端視覺化待實現）

### 完成路線圖
```
第1週 (5/5-5/11)   │ 會員考試UI完成 + 後端整合調試
第2週 (5/12-5/18)  │ 統計報表實現 + 全面功能測試
第3週 (5/19-5/25)  │ 性能優化 + 安全加固 + 集成測試
第4週 (5/26-6/1)   │ 生產部署準備 + 用戶驗收測試 (UAT)
第5週 (6/2-6/15)   │ 生產部署 + 性能監控 + 問題修復
```

---

## 一、詳細任務清單

### A. 會員考試作答 UI 完成（工作量：80小時 | 週期：4天）

#### A1. 修復與優化現有考試流程 - [Priority: CRITICAL]
**預估**: 16小時 | **負責**: 前端開發者 | **完成度**: 40%

**背景**: 
- 考試 UI 已存在 (`/public/member/index.html` 第282-562行)
- 後端 API 完整，但前端有多個缺陷待修復
- `exam-optimization-patch.js` 包含應該整合的修補

**具體工作**:

1. **整合計時器警告功能** (4小時)
   - 位置: `public/member/index.html` - `examsTab()` 函數的 `startTimeRemainingTimer()`
   - 當剩餘時間 < 5分鐘時顯示紅色警告
   - 實現從 `exam-optimization-patch.js` 複製的邏輯
   - 驗收標準:
     - [ ] 計時器每秒更新
     - [ ] 達到 5分鐘時警告出現
     - [ ] 警告文字清晰可見

2. **修復視窗切換作弊檢測** (5小時)
   - 實現 `visibilityChangeCount` 跟蹤
   - 與提交時一同發送給後端 (已在 `member-exams.js` 第655行實現)
   - 測試：多次切換視窗，應在第3次告警、第10次標記作弊
   - 驗收標準:
     - [ ] `visibilitychange` 事件正確偵測
     - [ ] 切換次數達 10 次時自動提交並標記 `auto_submitted_cheating`
     - [ ] 清理:清除 visibility listener (防止內存洩漏)

3. **修復時間計算與提交邏輯** (4小時)
   - 確保 `timeSpent` 正確計算: `(現在 - startedAt) / 1000` 秒
   - 驗證 `submitExam()` 發送完整的答案數組 (含所有必要欄位)
   - 檢查錯誤處理：網路失敗、超時、重複提交防護
   - 驗收標準:
     - [ ] 時間計算精確（誤差 < 1秒）
     - [ ] 網路中斷時顯示重試選項
     - [ ] 防止雙重提交

4. **修復證書 API 路由** (3小時)
   - 當前端點: `/api/member/exams/certificate/:certNumber` (第1823行)
   - 後端實現位置: `routes/member-exams.js` 約第 764-800行
   - **缺陷**: API 路由為 `/api/member/exams/:id/result` 但前端呼叫 `/api/member/exams/:examId/submit`
   - **需求**: 創建統一的 POST `/api/member/exams/:examId/submit/:attemptId` 端點
   - 驗收標準:
     - [ ] 證書下載 API 回傳 PDF 二進位
     - [ ] Content-Type 正確設置為 `application/pdf`
     - [ ] 檔案名包含證書編號

---

#### A2. 實現完整的考試作答流程 - [Priority: CRITICAL]
**預估**: 22小時 | **負責**: 前端開發者 | **完成度**: 60%

**背景**: 
- UI 骨架已存在
- 但多個互動邏輯需完善

**具體工作**:

1. **開始考試流程優化** (5小時)
   - 改進 `startExam()` 函數 (第1655行)
   - 添加加載狀態反饋: `startingExam === examId` 禁用按鈕
   - 驗收標準:
     - [ ] 點擊後按鈕變灰，顯示「開始中…」
     - [ ] API 回應後立即切換到考試界面
     - [ ] 錯誤信息以 red alert box 顯示
     - [ ] 5秒超時時顯示友善錯誤提示

2. **繼續作答流程實現** (6小時)
   - 實現 `resumeExam()` 函數 (第1694行)
   - 從 `/api/member/exams/:examId/resume` 獲取狀態
   - **關鍵邏輯**: 恢復前次未完成的答案
   ```javascript
   // 範例邏輯
   if (data.data.answers) {
       data.data.answers.forEach(a => {
           this.answers[a.questionId] = a.answer;
       });
   }
   ```
   - 驗收標準:
     - [ ] 頁面重載後能恢復之前填寫的答案
     - [ ] 計時器重新開始倒計時
     - [ ] 已提交的考試無法繼續

3. **選項隨機排列顯示** (6小時)
   - 從後端配置 `shuffleOptions` 和 `shuffleQuestions`
   - 如果啟用，在 UI 層進行再次排列 (已在後端評分前進行)
   - **重要**: 保持答案對應關係
   - 驗收標準:
     - [ ] 多次刷新頁面，選項順序可能不同
     - [ ] 答案提交時仍然正確對應
     - [ ] 難度等級保持不變

4. **答案保存與草稿機制** (5小時)
   - 實現 autosave (每30秒保存一次)
   - 或本地 localStorage 備份
   - 驗收標準:
     - [ ] 網頁關閉重開後，答案不遺失
     - [ ] 每次修改答案後，有視覺回饋 (如 checkmark)
     - [ ] 網路中斷時顯示警告

---

#### A3. 統一提交與結果展示 - [Priority: HIGH]
**預估**: 16小時 | **負責**: 前端開發者 | **完成度**: 70%

**背景**: 
- 提交邏輯已存在 (第1762行)
- 結果顯示已存在 (第546行)
- 但缺少細節錯誤處理與反饋

**具體工作**:

1. **提交確認與進度指示** (4小時)
   - 提示框: "確定要提交考卷嗎？" 已有
   - 改進: 顯示已答題目數 / 總題數
   ```javascript
   const answered = Object.keys(this.answers).length;
   const total = this.currentAttempt.questions.length;
   confirm(`確定要提交？已填寫 ${answered}/${total} 題`);
   ```
   - 驗收標準:
     - [ ] 提交按鈕禁用期間顯示「提交中…」
     - [ ] 未答題目數予以提示
     - [ ] 15秒內無回應時顯示「連接中…」

2. **結果頁面優化** (5小時)
   - 位置: 第546-562行
   - 展示內容:
     - 得分 + 百分比
     - 通過/未通過
     - 是否獲得證書
     - 下一步行動按鈕
   - 驗收標準:
     - [ ] 通過時 (綠色背景) 顯示「恭喜」與下載證書按鈕
     - [ ] 未通過時 (紅色背景) 顯示重考選項與等待冷卻期
     - [ ] 結果頁逗留 10秒後自動關閉或提示返回

3. **作弊判定結果展示** (4小時)
   - 當 `attempt.status === 'auto_submitted_cheating'` 時:
   - 顯示特殊訊息: "檢測到異常行為，此次考試不予評分"
   - 驗收標準:
     - [ ] 與通過/未通過不同的視覺處理
     - [ ] 清晰說明理由 (視窗切換過於頻繁)
     - [ ] 不頒發證書

4. **証書下載整合** (3小時)
   - 使用 `downloadCertificate()` 函數 (第1820行)
   - 已接合後端 PDF 生成
   - 驗收標準:
     - [ ] PDF 檔案下載成功
     - [ ] 檔案名格式: `certificate-ACTC-EXAM-2026-XXXXXX.pdf`
     - [ ] 中文字體正確顯示

---

#### A4. 歷史查看與明細 - [Priority: MEDIUM]
**預估**: 15小時 | **負責**: 前端開發者 | **完成度**: 50%

**背景**: 
- 歷史列表 UI 已存在 (第382-407行)
- 明細 modal 已存在 (第411-485行)
- API 端點 `/api/member/exams/:examId/result?attemptId=` 已實現

**具體工作**:

1. **歷史記錄展示改進** (5小時)
   - 列出所有已評分的考試 (`status === 'graded'`)
   - 包括自動作弊標記
   - 按日期倒序排列
   - 驗收標準:
     - [ ] 顯示嘗試次數、分數、通過狀態
     - [ ] 點擊可展開明細
     - [ ] 操作反應時間 < 1秒

2. **明細頁面完成** (6小時)
   - 實現 `loadAttemptDetail()` (第1854行)
   - 呼叫 `/api/member/exams/{examId}/result?attemptId={attemptId}`
   - 後端端點位置: `routes/member-exams.js` 約第720-760行
   - 顯示:
     - 摘要: 得分、結果、作答時間、通過分數
     - 題目明細: 題目內容、用戶答案、正確答案、是否作答
   - 驗收標準:
     - [ ] 載入時顯示 loading spinner
     - [ ] 題目答案以色編碼 (綠=正確, 紅=錯誤)
     - [ ] 無題目明細時顯示提示
     - [ ] 正確答案根據 `showCorrectAnswers` 配置隱藏/顯示

3. **統計數據補充** (4小時)
   - 在歷史記錄上方添加統計面板:
     - 總考試次數
     - 通過次數
     - 平均分數
     - 最高分
   - 驗收標準:
     - [ ] 實時計算
     - [ ] 每次考試完成後自動更新

---

#### A5. 行動裝置適配 - [Priority: MEDIUM]
**預估**: 11小時 | **負責**: 前端開發者 | **完成度**: 30%

**背景**: 
- 使用 Tailwind 響應式, 但未在手機上測試

**具體工作**:

1. **小屏幕測試與修復** (6小時)
   - 測試設備: iPhone SE (375px), iPhone 12 (390px), 平板 (768px)
   - 檢查:
     - 計時器文字在小屏是否可見
     - 題目與選項是否易點擊 (最小48x48px)
     - 提交按鈕位置是否合適
   - 驗收標準:
     - [ ] 375px 寬下無文字截斷
     - [ ] 所有按鈕可點擊 (無相鄰按鈕干擾)
     - [ ] 滾動流暢

2. **觸屏互動優化** (3小時)
   - 移除 hover 狀態 (在手機上不生效)
   - 添加 active 狀態反饋
   - 禁用雙擊縮放 (已選 radio 時)
   - 驗收標準:
     - [ ] 點擊選項時有即時反饋 (顏色變化)
     - [ ] 虛擬鍵盤不遮蓋輸入框
     - [ ] 單手操作可行

3. **網路狀態指示** (2小時)
   - 顯示網路連接狀態
   - 離線時提示無法提交
   - 驗收標準:
     - [ ] 有網路狀態圖標或文字
     - [ ] 離線時禁用提交按鈕

---

### B. 統計與報表前端實現（工作量：45小時 | 週期：3天）

#### B1. 後端統計 API 查驗 - [Priority: HIGH]
**預估**: 5小時 | **負責**: 後端/前端開發者 | **完成度**: 80%

**背景**: 
- 後端統計端點: `GET /api/exams/{id}/statistics` (routes/exams.js 約700-800行)
- 返回結構：`{ totalAttempts, passedCount, failedCount, averageScore, passRate, scoreDistribution, difficultyAnalysis }`

**具體工作**:

1. **驗證 API 回應格式** (2小時)
   - 執行: `curl -H "Authorization: Bearer {adminToken}" http://localhost:5001/api/exams/{examId}/statistics`
   - 檢查返回欄位完整性
   - 確保數值計算正確
   - 驗收標準:
     - [ ] 回應狀態 200
     - [ ] 所有必要欄位存在
     - [ ] 數值無 NaN/Infinity

2. **補充缺失欄位** (3小時)
   - 若缺 `scoreDistribution` (分數分佈): 實現 10% 級距統計
   - 若缺 `difficultyAnalysis`: 按難度統計通過率
   - 檢查 `createdBy` 過濾 (只顯示該管理員的考試)
   - 驗收標準:
     - [ ] 所有欄位數值邏輯正確
     - [ ] 無 SQL 注入風險

---

#### B2. 後台管理統計面板 - [Priority: HIGH]
**預估**: 20小時 | **負責**: 前端開發者 | **完成度**: 40%

**背景**: 
- 管理後台已有 modal 顯示統計 (public/admin/question-bank.html 第36行 `showStatistics`)
- 但視覺化圖表缺失

**具體工作**:

1. **統計數據卡片** (6小時)
   - 四個關鍵指標卡:
     - 總考試次數 (大文字)
     - 通過次數 (綠色)
     - 未通過次數 (紅色)
     - 平均分數 (藍色)
     - 通過率百分比 (突出)
   - 驗收標準:
     - [ ] 卡片排列美觀 (2x3 或 1x5 grid)
     - [ ] 數值實時更新
     - [ ] 有正確的色彩編碼

2. **分數分佈直方圖** (8小時)
   - 使用 Chart.js (CDN: https://cdn.jsdelivr.net/npm/chart.js)
   - X軸: 分數範圍 (0-10%, 10-20%, ..., 90-100%)
   - Y軸: 人數
   - 驗收標準:
     - [ ] 柱狀圖正確顯示
     - [ ] 懸停時顯示確切數值
     - [ ] 圖表響應式縮放

3. **難度分析圖表** (6小時)
   - 三個難度等級 (easy, medium, hard) 的通過率
   - 形式: 圓餅圖或水平條狀圖
   - 顯示: 難度名稱、通過率、人數
   - 驗收標準:
     - [ ] 三個難度清晰區分
     - [ ] 數值準確
     - [ ] 圖例清晰

---

#### B3. 會員側統計面板 - [Priority: MEDIUM]
**預估**: 12小時 | **負責**: 前端開發者 | **完成度**: 20%

**背景**: 
- 會員端考試頁已有歷史列表 (第382-407行)
- 但無整體統計面板

**具體工作**:

1. **個人統計摘要** (5小時)
   - 位置: 考試專區頂部
   - 顯示內容:
     - 參加考試次數
     - 通過次數
     - 最新成績
     - 最高成績
   - 驗收標準:
     - [ ] 卡片佈局美觀
     - [ ] 自動計算（無需手動輸入）

2. **進度視覺化** (4小時)
   - 通過率進度條 (e.g., 2/3 考試通過 = 67%)
   - 證書計數器 (已獲得 N 張證書)
   - 驗收標準:
     - [ ] 進度條顏色根據百分比變化 (紅→黃→綠)
     - [ ] 數值實時更新

3. **考試排名（可選）** (3小時)
   - 若時間允許: 顯示「您排名前 X%」
   - 基於同一考試的所有用戶分數
   - 驗收標準:
     - [ ] 不暴露其他用戶個人資訊
     - [ ] 僅顯示您參加過的考試排名

---

#### B4. 數據匯出功能 - [Priority: LOW]
**預估**: 6小時 | **負責**: 前端開發者 | **完成度**: 0%

**背景**: 
- 後端已有 `/api/exams/:id/export` CSV 端點

**具體工作**:

1. **CSV 下載按鈕** (3小時)
   - 在統計面板旁添加「匯出 CSV」按鈕
   - 呼叫 `/api/exams/{id}/export`
   - 下載為 `exam-{id}-results-{date}.csv`
   - 驗收標準:
     - [ ] 按鈕點擊後下載開始
     - [ ] CSV 檔案格式正確 (可在 Excel 開啟)

2. **PDF 報告（可選）** (3小時)
   - 若時間允許: 生成統計摘要 PDF
   - 包含統計卡片 + 圖表截圖
   - 驗收標準:
     - [ ] PDF 生成不阻礙 UI
     - [ ] 內容清晰

---

### C. 系統集成與測試（工作量：70小時 | 週期：5天）

#### C1. 端到端功能測試 - [Priority: CRITICAL]
**預估**: 25小時 | **負責**: QA / 開發者 | **完成度**: 0%

**測試場景 (使用 Playwright 自動化)**:

1. **考試正常流程** (5小時)
   ```
   1. 會員登入
   2. 進入考試專區
   3. 開始新考試 → 驗證隨機抽題
   4. 作答所有題目 → 驗證答案保存
   5. 提交 → 驗證評分正確
   6. 查看成績 → 驗證結果展示
   7. 下載證書 → 驗證 PDF
   8. 查看歷史 → 驗證明細
   ```
   - 測試工具: `scripts/test-question-bank-chrome.js` 為範本
   - 預期成功率: 100%

2. **邊界情況測試** (5小時)
   ```
   1. 時間限制: 考試倒計時結束 → 自動提交
   2. 視窗切換: 切換標籤10次 → 作弊檢測
   3. 網路中斷: 提交時斷網 → 顯示重試
   4. 重複提交: 點擊提交按鈕多次 → 防護成功
   5. 多瀏覽器: 同帳號兩個標籤 → 一個無法開始
   ```
   - 預期成功率: 100%

3. **會員權限測試** (5小時)
   ```
   1. 未驗證郵件: 無法提交 → 提示驗證
   2. 非核准會員: 無法查看 → 提示申請
   3. 已達最大次數: 無法開始 → 提示已滿
   4. 冷卻期: 通過後重考 → 提示冷卻期
   ```
   - 預期成功率: 100%

4. **管理員功能測試** (5小時)
   ```
   1. 建立考試 → 驗證狀態轉換
   2. 編輯考試 → 驗證限制 (只能編輯草稿)
   3. 查看統計 → 驗證數據準確
   4. 匯入題目 → 驗證批量導入
   5. 撤銷證書 → 驗證會員無法驗證
   ```
   - 預期成功率: 100%

5. **性能基準測試** (5小時)
   - 同時 10 個會員作答: 無延遲
   - 統計頁面加載時間: < 2秒
   - 題目搜尋: < 1秒
   - 證書 PDF 生成: < 5秒
   - 預期成功率: 100%

---

#### C2. 後端 API 完整性審計 - [Priority: CRITICAL]
**預估**: 15小時 | **負責**: 後端開發者 | **完成度**: 30%

**檢查清單**:

1. **缺失端點檢查** (5小時)
   - `GET /api/member/exams` - ✅ 已實現
   - `POST /api/member/exams/:id/start` - ✅ 已實現
   - `GET /api/member/exams/:id/resume` - ✅ 已實現
   - `POST /api/member/exams/:id/submit` - ✅ 已實現
   - `GET /api/member/certificates` - ❓ 需驗證 (應在 member-exams.js)
   - `GET /api/member/exams/certificate/:certNumber` - ❓ 需驗證
   - `GET /api/member/exams/:id/result` - ❓ 需驗證 (用於歷史明細)
   - `GET /api/certificates/verify/:certificateNumber` - ✅ 已實現 (server.js)
   - 驗收標準:
     - [ ] 所有 7 個會員端點均可測試
     - [ ] 無 404 回應

2. **回應格式統一** (5小時)
   - 檢查所有考試端點的回應結構
   - 應使用: `{ data, error: { code, message, details } }`
   - 非: `{ message }`
   - 驗收標準:
     - [ ] 一致的錯誤碼 (VALIDATION_ERROR, EXAM_NOT_FOUND 等)
     - [ ] 分頁資訊在列表 API 中: `{ pagination: { total, totalPages, page, limit } }`

3. **認證與授權驗證** (3小時)
   - 無效 token → 401
   - 權限不足 → 403
   - 過期 token → 401 + 提示重新登入
   - 驗收標準:
     - [ ] 所有受保護端點均檢查 JWT
     - [ ] 無繞過認證的路徑

4. **輸入驗證加強** (2小時)
   - 檢查 `/api/member/exams/:id/submit` 的答案驗證
   - 答案格式必須匹配題型:
     - multiple_choice: 整數 (0-3)
     - true_false: boolean
     - fill_in_blank: 字串
   - 驗收標準:
     - [ ] 錯誤格式被拒絕
     - [ ] 詳細錯誤訊息

---

#### C3. 資料庫一致性檢查 - [Priority: HIGH]
**預估**: 10小時 | **負責**: 後端開發者 | **完成度**: 20%

**檢查清單**:

1. **模型關係驗證** (3小時)
   - `Exam` ↔ `Question` (一對多)
   - `Exam` ↔ `ExamAttempt` (一對多)
   - `ExamAttempt` ↔ `Certificate` (一對一)
   - `User` ↔ `ExamAttempt` (一對多)
   - 驗收標準:
     - [ ] 無孤立記錄 (e.g., ExamAttempt 的 user 已刪)
     - [ ] 外鍵參考完整

2. **索引性能檢查** (3小時)
   - 檢查重要查詢是否有適當索引
   - 如: `db.exams.find({ status: 1, startDate: 1, endDate: 1 })`
   - 運行解釋計劃 (explain)
   - 驗收標準:
     - [ ] COLLSCAN 查詢 < 5%
     - [ ] 查詢響應時間 < 100ms

3. **資料完整性驗證** (2小時)
   - 運行檢查指令:
     ```javascript
     // 檢查孤立 ExamAttempt
     db.exams.aggregate([{$match:{deleted:{$ne:true}}}, {$lookup:{from:"examinattempts",foreignField:"exam",localField:"_id",as:"attempts"}}, {$match:{attempts:{$size:0}}}])
     
     // 檢查無效分數
     db.examinattempts.find({score:{$gt:100}})
     ```
   - 驗收標準:
     - [ ] 無孤立記錄
     - [ ] 無無效分數

4. **備份與恢復演練** (2小時)
   - 實施:
     1. 匯出生產資料 (使用 mongodump)
     2. 刪除測試資料
     3. 恢復備份 (使用 mongorestore)
     4. 驗證資料完整性
   - 驗收標準:
     - [ ] 備份包含所有考試資訊
     - [ ] 恢復後無資料遺失

---

#### C4. 前端跨瀏覽器相容性 - [Priority: MEDIUM]
**預估**: 10小時 | **負責**: 前端開發者 | **完成度**: 30%

**支持矩陣**:

| 瀏覽器 | 最小版本 | 測試 |
|--------|----------|------|
| Chrome | 90+ | ✅ |
| Safari | 14+ | ❓ |
| Firefox | 88+ | ❓ |
| Edge | 90+ | ❓ |
| Mobile Safari (iOS) | 14+ | ❓ |
| Chrome Mobile | 90+ | ❓ |

**測試工作**:

1. **CSS 相容性** (3小時)
   - 檢查 Tailwind CSS 是否在所有瀏覽器正常
   - 特別注意: `grid`, `flex`, `backdrop-blur`
   - 預期: 所有現代瀏覽器都支持

2. **JavaScript 相容性** (4小時)
   - Alpine.js 本身相容性好
   - 檢查:
     - `fetch()` API - ✅ 所有現代瀏覽器
     - `Intl.DateTimeFormat` - ⚠️ 需 polyfill (低端設備)
     - `localStorage` - ✅ 所有現代瀏覽器
   - 預期: 無重大問題

3. **表單輸入** (2小時)
   - 測試 `<input type="radio">`, `<input type="text">` 等
   - 特別在 iOS Safari 上:
     - 虛擬鍵盤是否顯示正確
     - 自動完成是否幹擾
   - 預期: 正常運作

4. **文字與字體** (1小時)
   - 檢查中文文字在各瀏覽器渲染
   - 特別是 PDF 中文字體
   - 預期: 正常顯示

---

#### C5. 安全性核查 - [Priority: CRITICAL]
**預估**: 10小時 | **負責**: 後端開發者 | **完成度**: 40%

**檢查清單**:

1. **認證與授權** (3小時)
   - JWT token 過期時間: 應設置 1-7天 (檢查 `JWT_SECRET` 使用)
   - Refresh token: 若有實現，檢查滾動更新邏輯
   - Session 劫持防護: HttpOnly cookies 配置 (已在 fixes/s2 中實現)
   - 驗收標準:
     - [ ] Token 過期檢查有效
     - [ ] 無在 localStorage 暴露敏感資訊

2. **輸入驗證與 XSS** (2小時)
   - 檢查題目內容中是否可注入 HTML/JS
   - 特別是在答案顯示時 (第467行)
   - 使用 DOMPurify 或類似庫過濾
   - 驗收標準:
     - [ ] `<img src=x onerror=alert()>` 無法執行

3. **SQL/NoSQL 注入** (2小時)
   - 檢查所有 API 是否正確使用 Mongoose (自動防護)
   - 特別檢查 `search` 參數
   - 驗收標準:
     - [ ] 無 `eval()` 或動態查詢
     - [ ] 所有欄位驗證

4. **CSRF 與 CORS** (2小時)
   - CSRF: Express 已配置 Helmet (檢查 CSRF token)
   - CORS: 檢查白名單是否過寬
   - 驗收標準:
     - [ ] 跨域請求需正確的 Origin header
     - [ ] POST 請求需 CSRF token (若有)

5. **費率限制（Rate Limiting）** (1小時)
   - 檢查考試提交是否有費率限制 (防止暴力破解)
   - 檢查登入是否有限制 (已在 auth 中實現)
   - 驗收標準:
     - [ ] 1 分鐘內最多提交 3 次考試
     - [ ] 無法通過 API 暴力遍歷所有答案

---

### D. 性能優化（工作量：35小時 | 週期：3天）

#### D1. 資料庫查詢優化 - [Priority: HIGH]
**預估**: 10小時 | **負責**: 後端開發者 | **完成度**: 40%

**優化項目**:

1. **N+1 查詢消除** (5小時)
   - `GET /api/member/exams` 時，查詢所有考試 + 所有用戶嘗試
   - 現狀 (routes/member-exams.js 76-88行):
     ```javascript
     const exams = await Exam.find(...)
     const attempts = await ExamAttempt.find({exam: {$in: examIds}})
     // 已優化為 2 個查詢，很好！
     ```
   - 檢查其他端點是否有類似問題
   - 目標: 所有列表查詢 ≤ 2 次 DB 調用
   - 驗收標準:
     - [ ] 無迴圈內 DB 查詢

2. **投影最小化** (3小時)
   - 只查詢必要欄位 (使用 `.select()`)
   - 例如: 列表 API 不需要完整題目內容
   - 目標: 減少網路傳輸 30%
   - 驗收標準:
     - [ ] API 回應大小 < 1MB

3. **查詢緩存** (2小時)
   - 考試列表可緩存 5分鐘 (不常變更)
   - 統計資料可緩存 10分鐘
   - 題目快照無需緩存 (已在 ExamAttempt 中儲存)
   - 使用 Redis (若部署支持) 或記憶體快取
   - 驗收標準:
     - [ ] 重複查詢時 < 50ms

---

#### D2. 前端資源優化 - [Priority: MEDIUM]
**預估**: 10小時 | **負責**: 前端開發者 | **完成度**: 30%

**優化項目**:

1. **圖片優化** (3小時)
   - 考試卡片圖片使用 WebP (若支持)
   - 或使用佔位符避免版面移位
   - 證書 PDF 中的 logo 應使用最小化版本
   - 目標: 首屏加載 < 2秒
   - 驗收標準:
     - [ ] 圖片總大小 < 200KB

2. **JavaScript 分割** (4小時)
   - Alpine.js 已 CDN 載入 (無法優化)
   - 但自定義 JS 可延遲載入:
     - `exam-optimization-patch.js` 僅在考試頁面載入
     - 圖表庫 Chart.js 僅在統計頁載入
   - 目標: 會員頁首屏 JS < 100KB
   - 驗收標準:
     - [ ] 主 HTML 不含圖表代碼

3. **CSS 精簡** (2小時)
   - Tailwind 已是 CDN (無法精簡)
   - 但自定義 CSS 應移除未使用的
   - 檢查 `common.css` 中的規則是否全部使用
   - 目標: 自定義 CSS < 20KB
   - 驗收標準:
     - [ ] CSS 檔案大小 < 50KB

4. **緩存策略** (1小時)
   - 靜態資源 (JS, CSS) 應設置 1 年快取 (使用版本號)
   - HTML 頁面應設置 no-cache (檢查內容更新)
   - API 回應應根據內容設置 Cache-Control
   - 驗收標準:
     - [ ] `index.html` 每次都驗證
     - [ ] `common.js` 緩存 1 年

---

#### D3. API 響應時間改進 - [Priority: MEDIUM]
**預估**: 10小時 | **負責**: 後端開發者 | **完成度**: 30%

**基準測試與目標**:

| 端點 | 現狀 | 目標 | 優化方案 |
|------|------|------|---------|
| `GET /api/member/exams` | ? | < 500ms | 查詢優化 + 緩存 |
| `POST /api/member/exams/:id/start` | ? | < 1s | 非同步題目隨機化 |
| `POST /api/member/exams/:id/submit` | ? | < 2s | 非同步評分 + 通知 |
| `GET /api/exams/:id/statistics` | ? | < 1s | 聚合管道優化 |

**具體工作**:

1. **建立性能基準** (3小時)
   - 使用 Artillery 或 k6 進行負載測試
   - 記錄當前各 API 的 P50, P95, P99 延遲
   - 驗收標準:
     - [ ] 基準報告已生成

2. **慢查詢日誌** (2小時)
   - 啟用 MongoDB 慢查詢日誌 (> 100ms 的查詢)
   - 檢查是否有意外的複雜查詢
   - 驗收標準:
     - [ ] 慢查詢 < 5% 的總查詢

3. **非同步優化** (3小時)
   - 評分與通知應非同步進行 (已在 submit 中實現)
   - 證書生成應在背景進行 (若超過 1s)
   - 使用 `Promise.all()` 並發相關操作
   - 驗收標準:
     - [ ] 提交考卷 API 回應 < 2s (不等待 PDF 生成)

4. **連接池管理** (2小時)
   - 檢查 Mongoose 連接池大小 (預設 10)
   - 在高負載時可增加到 20-30
   - 驗收標準:
     - [ ] 並發 100 請求無延遲增長

---

#### D4. 前端互動性能 - [Priority: MEDIUM]
**預估**: 5小時 | **負責**: 前端開發者 | **完成度**: 50%

**優化項目**:

1. **長列表渲染** (2小時)
   - 題目列表若超過 50 題，使用虛擬滾動
   - 使用 x-for key binding 確保正確更新
   - 驗收標準:
     - [ ] 100 題頁面滾動 FPS > 50

2. **輸入去抖動** (2小時)
   - 搜尋欄應使用 debounce (300ms)
   - 自動保存答案應使用 debounce (1s)
   - 驗收標準:
     - [ ] 搜尋時無多次 API 調用

3. **記憶體洩漏檢查** (1小時)
   - 使用 Chrome DevTools Memory 檢查
   - 特別檢查計時器清理 (visibility change listener)
   - 驗收標準:
     - [ ] 開啟/關閉考試多次後，記憶體無遞增

---

### E. 部署與準備（工作量：30小時 | 週期：4天）

#### E1. 生產環境檢查 - [Priority: CRITICAL]
**預估**: 8小時 | **負責**: DevOps / 後端開發者 | **完成度**: 20%

**檢查清單**:

1. **環境變數配置** (2小時)
   - 檢查所有必要變數 (JWT_SECRET, MONGO_URI, SMTP_*, CRON_*)
   - 敏感變數不應在代碼中
   - 驗收標準:
     - [ ] `.env.example` 已更新
     - [ ] 所有變數在部署指南中列出

2. **MongoDB Atlas 配置** (2小時)
   - 檢查連接字符串安全性
   - 啟用 IP 白名單 (僅允許部署機器)
   - 啟用自動備份 (每日)
   - 驗收標準:
     - [ ] 備份保留 7 天
     - [ ] 可復原測試通過

3. **字體檔案部署** (2小時)
   - 確保 `fonts/NotoSansCJKtc-*.ttf/.otf` 包含在構建中
   - 或上傳到 CDN 並更新路徑
   - 驗收標準:
     - [ ] 證書 PDF 中文顯示正常

4. **HTTPS 配置** (2小時)
   - 檢查 SSL 證書有效期
   - 啟用 HSTS header
   - 檢查重定向 (HTTP → HTTPS)
   - 驗收標準:
     - [ ] 無混合內容警告 (HTTPS + HTTP)

---

#### E2. 監控與告警設置 - [Priority: HIGH]
**預估**: 10小時 | **負責**: DevOps | **完成度**: 10%

**監控指標**:

1. **應用層監控** (4小時)
   - 設置 error rate 告警 (> 1% 觸發)
   - API 響應時間告警 (P95 > 2s)
   - 資料庫連接池飽和告警
   - 使用工具: Render 內建監控, CloudWatch, 或 Datadog
   - 驗收標準:
     - [ ] 告警郵件已設置

2. **基礎設施監控** (3小時)
   - CPU 使用率 (> 80% 告警)
   - 記憶體使用率 (> 85% 告警)
   - 磁碟空間 (< 10% 剩餘告警)
   - 驗收標準:
     - [ ] 儀表板已配置

3. **業務指標追蹤** (2小時)
   - 日考試次數
   - 日通過人數
   - 日證書核發數
   - 使用 Google Analytics 或 Mixpanel
   - 驗收標準:
     - [ ] 儀表板展示關鍵指標

4. **日誌聚合** (1小時)
   - 所有應用日誌應集中儲存
   - 設置關鍵錯誤關鍵字警報
   - 使用工具: ELK Stack 或 Papertrail
   - 驗收標準:
     - [ ] 可搜尋所有應用日誌

---

#### E3. 灾難恢復方案 - [Priority: HIGH]
**預估**: 8小時 | **負責**: DevOps / 後端開發者 | **完成度**: 20%

**恢復時間目標 (RTO) / 恢復點目標 (RPO)**:

| 場景 | RTO | RPO | 方案 |
|------|-----|-----|------|
| 應用伺服器故障 | < 5 min | 無資料遺失 | 自動重啟 + 藍綠部署 |
| 資料庫故障 | < 1 hour | < 1 hour | 自動備份 + 時間點恢復 |
| 資料中心故障 | < 4 hours | < 1 hour | 跨區域備份 (成本考量) |
| 應用程式 bug | < 30 min | 無資料遺失 | 快速回滾 + 修復 |

**具體工作**:

1. **備份策略** (3小時)
   - MongoDB 每日自動備份 (可在 Atlas 設置)
   - 保留 30 天備份
   - 測試恢復流程 (每月執行一次)
   - 驗收標準:
     - [ ] 備份清單已記錄
     - [ ] 恢復時間 < 1 小時

2. **回滾計劃** (3小時)
   - Git 標籤用於每個部署 (如 `v1.0.0-prod`)
   - 部署指令應支持回滾: `npm run rollback -- v1.0.0-prod`
   - 驗收標準:
     - [ ] 回滾指令已測試

3. **故障通知** (2小時)
   - 設置自動告警於關鍵故障 (DB 離線, 應用崩潰)
   - 通知方式: Email + Slack (若有)
   - 驗收標準:
     - [ ] 告警已觸發測試

---

#### E4. 用戶驗收測試 (UAT) - [Priority: MEDIUM]
**預估**: 4小時 | **負責**: 項目經理 / 客戶 | **完成度**: 0%

**UAT 計劃**:

1. **測試用戶準備** (1小時)
   - 創建 3-5 個測試帳號 (含不同角色: 會員, 管理員)
   - 準備考試測試數據 (3 份考試, 各 20 題)
   - 驗收標準:
     - [ ] 帳號密碼已交付客戶

2. **用戶測試腳本** (2小時)
   - 提供逐步測試說明文檔
   - 包含截圖與預期結果
   - 允許客戶自由探索與反饋
   - 驗收標準:
     - [ ] 客戶反饋表已完成
     - [ ] 無阻礙性 bug

3. **反饋收集與修復** (1小時)
   - 蒐集所有反饋
   - 優先級分類 (Critical / High / Medium / Low)
   - Critical 立即修復, Medium 可延後
   - 驗收標準:
     - [ ] 反饋已分類和跟蹤

---

### F. 文件與交付（工作量：20小時 | 週期：持續）

#### F1. 技術文檔更新 - [Priority: MEDIUM]
**預估**: 8小時 | **負責**: 技術寫手 / 開發者 | **完成度**: 70%

**文檔清單**:

1. **API 文檔** (`docs/exam-system/API.md`)
   - ✅ 已有完整文檔
   - 需補充: 會員端 `/result` 端點 (第3.3節)
   - 需補充: 所有錯誤碼範例
   - 驗收標準:
     - [ ] 所有 API 可複製即用

2. **系統規格書** (`docs/exam-system/SPEC.md`)
   - ✅ 已有完整規格
   - 需更新: v1.1.0 (若有功能變更)
   - 需補充: 性能基準 (< 2s 響應時間)
   - 驗收標準:
     - [ ] 規格書與實現 100% 一致

3. **部署指南** (`DEPLOYMENT.md`)
   - ❌ 不存在或不完整
   - 需包含:
     - 環境要求 (Node.js 18+, MongoDB, npm 9+)
     - 環境變數設置範例
     - 初始化步驟 (npm install, seed data)
     - Render 部署步驟
     - 生產環境檢查清單
   - 驗收標準:
     - [ ] 新開發者可按步驟成功部署

4. **故障排除指南** (`docs/exam-system/TROUBLESHOOTING.md`)
   - 常見問題:
     - "為什麼我的考試沒有出現？" → 檢查 `status` 和時間範圍
     - "為什麼評分錯誤？" → 檢查題目類型和 `shuffleOptions` 配置
     - "PDF 生成失敗？" → 檢查字體檔案
   - 驗收標準:
     - [ ] 覆蓋 10+ 常見問題

---

#### F2. 用戶指南 - [Priority: LOW]
**預估**: 6小時 | **負責**: 技術寫手 | **完成度**: 0%

**指南清單**:

1. **會員用戶指南**
   - 如何參加考試
   - 如何查看成績
   - 如何下載證書
   - 驗收標準:
     - [ ] 包含截圖

2. **管理員操作指南**
   - 如何創建考試
   - 如何管理題庫
   - 如何查看統計
   - 驗收標準:
     - [ ] 包含操作步驟

---

#### F3. 變更日誌 - [Priority: LOW]
**預估**: 2小時 | **負責**: 開發者 | **完成度**: 0%

**CHANGELOG.md** 應記錄:
```
## [1.0.0] - 2026-06-15
### Added
- 會員端完整考試作答流程
- 統計與報表視覺化
- 證書驗證與下載

### Fixed
- 計時器警告顯示
- 作弊檢測邏輯

### Security
- 啟用 HTTPS 強制
- JWT token 驗證
```

---

## 二、時間表與里程碑

### 高層時間表

```
Week 1 (5/5-5/11)
├─ Mon: 計時器 + 視窗切換檢測整合 (8h)
├─ Tue: 考試流程 (startExam, resumeExam) 完成 (6h)
├─ Wed: 提交與結果展示完成 (6h)
├─ Thu: 歷史與明細完成 (6h)
└─ Fri: 行動適配測試與修復 (6h)

Week 2 (5/12-5/18)
├─ Mon: 統計面板實現 (8h)
├─ Tue: 圖表整合 (Chart.js) (8h)
├─ Wed: 端到端測試開始 (8h)
├─ Thu: 後端 API 審計 (8h)
└─ Fri: 功能修復與調整 (6h)

Week 3 (5/19-5/25)
├─ Mon: 資料庫最佳化 (8h)
├─ Tue: 安全性核查 (8h)
├─ Wed: 性能測試與優化 (8h)
├─ Thu: 跨瀏覽器測試 (6h)
└─ Fri: 文件更新完成 (4h)

Week 4 (5/26-6/1)
├─ Mon: 生產環境部署準備 (8h)
├─ Tue: 監控告警設置 (8h)
├─ Wed: UAT 執行 (4h)
├─ Thu: 反饋修復 (8h)
└─ Fri: 部署彩排 (4h)

Week 5 (6/2-6/15)
├─ Mon-Fri: 生產部署 + 現場支持 (40h)
└─ 後續: 監控 + 熱修復 (按需)
```

### 關鍵里程碑

| 日期 | 里程碑 | 檢驗點 |
|------|--------|---------|
| 5/9 | 會員考試 UI 可用 | 從會員帳號完成一次完整考試 |
| 5/16 | 統計面板完成 | 管理員能看到考試統計圖表 |
| 5/23 | 全功能測試通過 | 所有 QA 測試用例 100% 通過 |
| 5/30 | 生產環境就緒 | 備份 + 監控 + 文檔完成 |
| 6/15 | 生產部署 | 實際用戶可訪問系統 |

---

## 三、風險評估與緩解

### 高風險 (Risk Level: HIGH)

#### R1. 會員端 UI 複雜度高於預期
**影響**: 時間超支 2-3 週  
**觸發條件**: Alpine.js 狀態管理複雜, 計時器與事件監聽多  
**緩解方案**:
- 優先完成核心流程 (開始→作答→提交)
- 暫時跳過視覺化增強 (圖表)
- 可用模態代替多頁面切換 (減少狀態)
- 預留 20% 時間緩衝 (已在時間表中)

#### R2. 資料庫性能問題
**影響**: 統計查詢超時, 無法支持並發  
**觸發條件**: MongoDB 查詢計劃不當, 無適當索引  
**緩解方案**:
- Week 3 進行負載測試, 及時發現
- 如有問題, 考慮遷移至 PostgreSQL (成本高)
- 短期: 使用緩存層 (Redis, 若部署支持)

#### R3. 後端 API 錯誤或缺失
**影響**: 前端卡頓, 無法完成整合  
**觸發條件**: API 文檔與實現不符  
**緩解方案**:
- Week 1 進行全面 API 審計
- 建立前後端溝通機制 (每日同步)
- 必要時調整前端假數據進行開發

#### R4. 部署環境問題
**影響**: 無法在生產上線, 導致延期  
**觸發條件**: 字體檔案遺失, MongoDB 連接失敗, SMTP 未配置  
**緩解方案**:
- Week 4 進行完整部署模擬
- 建立部署檢查清單 (已在 E1 中)
- 保持快速回滾計劃 (已在 E3 中)

### 中風險 (Risk Level: MEDIUM)

#### R5. 客戶 UAT 反饋過多
**影響**: 時間超支 1-2 週  
**緩解方案**:
- 提前與客戶對齊期望 (Week 1)
- 區分 Critical vs Nice-to-have
- Critical bug 優先修復

#### R6. 跨瀏覽器相容性問題
**影響**: 部分用戶無法使用  
**緩解方案**:
- 在 iOS Safari 上優先測試
- 準備降級方案 (e.g., 禁用 advanced features)

### 低風險 (Risk Level: LOW)

#### R7. 團隊成員病假
**影響**: 進度延遲  
**緩解方案**:
- 文檔完整, 易於知識轉移
- 關鍵任務預留備選人員

---

## 四、資源與團隊結構

### 建議團隊配置

```
項目經理 (1人)
├─ 全程監督進度
├─ 客戶溝通
└─ 風險管理

前端開發者 (2人)
├─ 開發者 A: 會員考試 UI 實現 (A1-A5)
└─ 開發者 B: 統計面板 + 圖表 (B2-B4)

後端開發者 (1人)
├─ API 審計與修復 (C2)
├─ 資料庫優化 (D1)
└─ 性能基準測試

QA 工程師 (1人)
├─ 編寫測試用例 (C1)
├─ 回歸測試
└─ UAT 協調

DevOps/系統管理員 (1人, 兼職)
├─ 部署準備 (E1)
├─ 監控告警 (E2)
└─ 災難恢復 (E3)

技術寫手 (0.5人, 兼職)
└─ 文件與指南 (F1-F3)
```

### 總工時投入

```
前端開發      : 120 小時 (2人 x 6週)
後端開發      : 50 小時 (1人 x 6週)
QA 測試       : 40 小時 (1人 x 5週)
項目管理      : 30 小時 (1人 x 6週)
DevOps        : 20 小時 (0.5人 x 4週)
技術寫手      : 10 小時 (0.5人 x 2週)
─────────────────────
總計          : 270 小時 (平均 4.5人 x 6週)
```

---

## 五、質量保證與驗收標準

### 功能驗收標準

| 功能模塊 | 驗收標準 |
|----------|----------|
| 會員考試作答 | 從登入→完成→查看結果 100% 可達成 |
| 統計報表 | 所有圖表顯示正確, 數據精準 |
| 後端 API | 所有端點可用, 無 500 錯誤 |
| 安全性 | 無 XSS / SQL 注入 / CSRF 漏洞 |
| 性能 | API P95 延遲 < 2s, 前端 FCP < 2s |

### 測試覆蓋率目標

- 後端單元測試: 70%+ 代碼覆蓋
- 前端集成測試: 15+ 關鍵用戶路徑
- E2E 測試: 8+ 完整流程
- 手動 QA 測試: 50+ 測試用例

### 生產上線檢查清單

- [ ] 所有代碼已 code review
- [ ] 所有測試已通過
- [ ] 文檔已更新
- [ ] 備份已測試
- [ ] 監控已配置
- [ ] 客戶已簽字驗收
- [ ] 支援團隊已培訓
- [ ] 回滾計劃已準備

---

## 六、成功指標 (KPI)

### 交付指標

- 按時完成率: 目標 95% (允許 3-5 天延期)
- 測試通過率: 目標 99%+ (< 0.5% 缺陷逃逸)
- 文檔完成度: 目標 100%

### 業務指標 (上線後 30 天)

- 日活躍用戶: > 50
- 日考試完成數: > 20
- 一次性通過率: > 70%
- 用戶滿意度 (NPS): > 40

### 技術指標

- API 可用性: > 99.5%
- 平均響應時間: < 500ms
- P95 響應時間: < 2s
- 錯誤率: < 0.1%

---

## 七、依賴性與前置條件

### 外部依賴

1. **MongoDB Atlas 帳號**
   - 需要已創建且配置完整
   - 備份已啟用

2. **Render.com 帳號**
   - 已部署或準備就緒
   - 環境變數已設置

3. **郵件服務 (SMTP)**
   - 若啟用郵件通知，需配置 SMTP 伺服器
   - 推薦: SendGrid 或 Gmail

4. **CDN (可選)**
   - 若優化圖片加載，需 CDN 帳號
   - 推薦: Cloudflare 或 AWS CloudFront

### 內部依賴

1. **代碼庫狀態**
   - 主分支 (`main`) 必須穩定
   - 所有 PR 已合併

2. **測試數據**
   - 需要至少 3 份不同難度的考試
   - 需要至少 50 個測試題目

3. **人員可用性**
   - 所有團隊成員在計畫期間全職投入
   - 避免假期衝突

---

## 附錄 A: 技術棧決策記錄

### 為何選擇 Alpine.js 而非 Vue/React?

- 現有代碼已使用 Alpine.js
- 無構建步驟，部署簡單
- 適合中等複雜度的前端任務

### 為何選擇 Chart.js 而非 D3/Apache ECharts?

- 輕量級 (< 100KB)
- 無依賴，易整合到靜態 HTML
- 對基本圖表支持良好

### 為何不遷移至 TypeScript?

- 現有代碼為 vanilla JS
- 收益 vs 成本不足以支持大型重構
- 優先完成功能，後續可考慮

---

## 附錄 B: 假設與限制

### 假設

1. 部署環境 (Render) 穩定可用
2. MongoDB Atlas 連接穩定
3. 字體檔案已在代碼庫中
4. 客戶反饋會及時提供
5. 無重大業務需求變更

### 限制

1. 無專業 QA 測試環境 (使用開發環境)
2. 無真實用戶負載測試 (使用模擬)
3. 無國際化支持 (限 zh-TW)
4. 暫無行動應用 (網頁版本)
5. 考試系統功能鎖定 (不接受新功能請求直到 v1.0 完成)

---

## 附錄 C: 詞彙表

| 詞彙 | 定義 |
|------|------|
| Attempt | 用戶參加考試的一次嘗試記錄 |
| Cooldown | 通過考試後需等待的時間 |
| Cheating Detection | 基於視窗切換和時間的作弊檢測 |
| Certificate Number | 証書編號，格式 ACTC-EXAM-YYYY-XXXXXX |
| Difficulty Ratio | 考試中簡單/中等/困難題目的比例 |
| UAT | 用戶驗收測試 |

---

## 附錄 D: 更新日誌

| 日期 | 版本 | 變更 |
|------|------|------|
| 2026-05-03 | 1.0 | 初版發佈 |

---

**計劃製定人**: Claude PM Agent  
**審核人**: (待定)  
**核准人**: (待定)  

**下次審查日期**: 2026-05-12 (Week 1 結束)  
**計畫更新頻率**: 每週週五

---

**附註**: 此計劃基於當前的代碼審計與需求分析。如有重大變更或風險發生，應立即與項目經理溝通並更新計劃。

